import styled from "styled-components";
import { Dialog, FormControl, FilledInput } from "@mui/material";

export const StyledUpdateDialog = styled(Dialog)`
  /* Your custom styles for the dialog go here */
  .MuiDialog-paper {
    background-color: #fff;
    border-radius: 15px;
  }
`;

export const StyledImageDialog = styled(Dialog)`
  /* Your custom styles for the dialog go here */
  .MuiDialog-paper {
    background-color: #fff;
    border-radius: 15px;
  }
`;

export const ChangePasswordDialog = styled(Dialog)`
  /* Your custom styles for the dialog go here */
  .MuiDialog-paper {
    background-color: #fff;
    border-radius: 15px;
  }
`;

export const StyledFormControl = styled(FormControl)`
  background-color: #ebf4fd;
  border-radius: 10px;
  overflow: hidden;
`;
export const StyledFilledInput = styled(FilledInput)`
  border: 0;
`;
